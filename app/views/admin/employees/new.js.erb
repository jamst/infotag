$('#employee_modal').remove();
$('body').append('<div class="ui modal" id="employee_modal"><i class="close icon"></i><h2 class="ui dividing header">添加信息流</h2><%= j(render 'new') %></div>');
$('#employee_modal').modal('setting', {autofocus: false}).modal('show');
$('select.dropdown').dropdown();
$('#calendar1').calendar({type: 'date'});

// TODO表单校验
$("#commition").click(function () {
    if ($("#employee_name").val() != "") {
        $("#new_employee").submit();
    } else {
        $("#form_message").text("请完善信息！").show(200);
    }
});

$('#recommend_modal').remove();
$('body').append('<div class="ui modal" id="recommend_modal"><i class="close icon"></i><h2 class="ui dividing header">修改推荐</h2><%= j(render 'edit') %></div>');
$('#recommend_modal').modal('setting', {autofocus: false}).modal('show');
$('select.dropdown').dropdown();

// TODO表单校验
$("#commition").click(function () {
    if ($("#recommend_title").val() != "") {
        $("#edit_recommend_<%= @recommend.id %>").submit();
    } else {
        $("#form_message").text("请完善信息！").show(200);
    }
});

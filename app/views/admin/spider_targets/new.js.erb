$('#spider_target_modal').remove();
$('body').append('<div class="ui modal" id="spider_target_modal"><i class="close icon"></i><h2 class="ui dividing header">添加视频</h2><%= j(render 'new') %></div>');
$('#spider_target_modal').modal('setting', {autofocus: false}).modal('show');
$('select.dropdown').dropdown();
$('#calendar1').calendar({type: 'date'});

// TODO表单校验
$("#commition").click(function () {
    if ($("#spider_target_title").val() != "") {
        $("#new_spider_target").submit();
    } else {
        $("#form_message").text("请完善信息！").show(200);
    }
});

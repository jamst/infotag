$('#medial_cache_modal').remove();
$('body').append('<div class="ui modal" id="medial_cache_modal"><i class="close icon"></i><h2 class="ui dividing header">修改分享设置</h2><%= j(render 'edit') %></div>');
$('#medial_cache_modal').modal('setting', {autofocus: false}).modal('show');
$('select.dropdown').dropdown();
$('#calendar1').calendar({type: 'date'});

// TODO表单校验
$("#commition").click(function () {
    if ($("#medial_cache_title").val() != "") {
        $("#edit_medial_cache_<%= @medial_cache.id %>").submit();
    } else {
        $("#form_message").text("请完善信息！").show(200);
    }
});

$('#video_modal').remove();
$('body').append('<div class="ui modal" id="video_modal"><i class="close icon"></i><h2 class="ui dividing header">添加视频</h2><%= j(render 'new') %></div>');
$('#video_modal').modal('setting', {autofocus: false}).modal('show');
$('select.dropdown').dropdown();
$('#calendar1').calendar({type: 'date'});

// TODO表单校验
$("#commition").click(function () {
    if ($("#video_title").val() != "") {
        $("#new_video").submit();
    } else {
        $("#form_message").text("请完善信息！").show(200);
    }
});

$('#video_modal').remove();
$('body').append('<div class="ui modal" id="video_modal"><i class="close icon"></i><h2 class="ui dividing header">审核</h2><%= j(render 'to_approves') %></div>');
$('#video_modal').modal('setting', {autofocus: false}).modal('show');
$('select.dropdown').dropdown();
$('#calendar1').calendar({type: 'date'});

// TODO表单校验
$("#commition").click(function () {
    if ($("#video_approve_status").val() != "") {
        $("#new_video").submit();
    } else {
        $("#form_message").text("请完善信息！").show(200);
    }
});

$('#medial_spider_modal').remove();
$('body').append('<div class="ui modal" id="medial_spider_modal"><i class="close icon"></i><h2 class="ui dividing header">添加任务</h2><%= j(render 'new') %></div>');
$('#medial_spider_modal').modal('setting', {autofocus: false}).modal('show');
$('select.dropdown').dropdown();
$('#calendar1').calendar({type: 'date'});

// TODO表单校验
$("#commition").click(function () {
    if ($("#medial_spider_title").val() != "") {
        $("#new_medial_spider").submit();
    } else {
        $("#form_message").text("请完善信息！").show(200);
    }
});

$('#category_modal').remove();
$('body').append('<div class="ui modal" id="category_modal"><i class="close icon"></i><h2 class="ui dividing header">添加视频</h2><%= j(render 'new') %></div>');
$('#category_modal').modal('setting', {autofocus: false}).modal('show');
$('select.dropdown').dropdown();
$('#calendar1').calendar({type: 'date'});

// TODO表单校验
$("#commition").click(function () {
    if ($("#category_title").val() != "") {
        $("#new_category").submit();
    } else {
        $("#form_message").text("请完善信息！").show(200);
    }
});

$('#tag_modal').remove();
$('body').append('<div class="ui modal" id="tag_modal"><i class="close icon"></i><h2 class="ui dividing header">修改视频信息</h2><%= j(render 'edit') %></div>');
$('#tag_modal').modal('setting', {autofocus: false}).modal('show');
$('select.dropdown').dropdown();

// TODO表单校验
$("#commition").click(function () {
    if ($("#tag_title").val() != "") {
        $("#edit_tag_<%= @tag.id %>").submit();
    } else {
        $("#form_message").text("请完善信息！").show(200);
    }
});

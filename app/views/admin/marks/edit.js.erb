$('#mark_modal').remove();
$('body').append('<div class="ui modal" id="mark_modal"><i class="close icon"></i><h2 class="ui dividing header">修改标注</h2><%= j(render 'edit') %></div>');
$('#mark_modal').modal('setting', {autofocus: false}).modal('show');
$('select.dropdown').dropdown();

// TODO表单校验
$("#commition").click(function () {
    if ($("#mark_title").val() != "") {
        $("#edit_mark_<%= @mark.id %>").submit();
    } else {
        $("#form_message").text("请完善信息！").show(200);
    }
});

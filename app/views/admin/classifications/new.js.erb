$('#classification_modal').remove();
$('body').append('<div class="ui modal" id="classification_modal"><i class="close icon"></i><h2 class="ui dividing header">添加分类</h2><%= j(render 'new') %></div>');
$('#classification_modal').modal('setting', {autofocus: false}).modal('show');
$('select.dropdown').dropdown();
$('#calendar1').calendar({type: 'date'});

// TODO表单校验
$("#commition").click(function () {
    if ($("#classification_title").val() != "") {
        $("#new_classification").submit();
    } else {
        $("#form_message").text("请完善信息！").show(200);
    }
});
